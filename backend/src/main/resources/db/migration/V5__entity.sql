insert into entity_table (
    entity_id,
    entity_name
)values (
    nextval('entity_seq'),
    'none'
);

insert into entity_table (
    entity_id,
    entity_name
)values (
    nextval('entity_seq'),
    'Super Motors Corporation'
);

insert into entity_table (
    entity_id,
    entity_name
)values (
    nextval('entity_seq'),
    'All Suits Corporation'
);

insert into entity_table (
    entity_id,
    entity_name
)values (
    nextval('entity_seq'),
    'Landed Gentleman Corporation'
);

insert into entity_table (
    entity_id,
    entity_name
)values (
    nextval('entity_seq'),
    'Aquarium Corporation'
);

insert into entity_table (
    entity_id,
    entity_name
)values (
    nextval('entity_seq'),
    'Choco Corporation'
);

insert into entity_table (
    entity_id,
    entity_name
)values (
    nextval('entity_seq'),
    'White Collar Corporation'
);
